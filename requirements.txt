# Core -- needed for the lab to run offline (the default).
pydantic>=2.0

# Tests.
pytest>=8.0

# Optional -- only needed for real model calls (the --real flag). Safe to install.
litellm>=1.40
python-dotenv>=1.0
